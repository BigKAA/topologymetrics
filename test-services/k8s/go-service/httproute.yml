apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: go-service
  namespace: dephealth-test
  labels:
    app: go-service
spec:
  parentRefs:
    - name: eg
      namespace: envoy-gateway-system
  hostnames:
    - test1.kryukov.lan
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: go-service
          port: 8080
