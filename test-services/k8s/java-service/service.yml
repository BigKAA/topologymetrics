apiVersion: v1
kind: Service
metadata:
  name: java-service
  namespace: dephealth-test
  labels:
    app: java-service
spec:
  selector:
    app: java-service
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  type: ClusterIP
