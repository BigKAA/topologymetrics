apiVersion: v1
kind: Service
metadata:
  name: python-service
  namespace: dephealth-test
  labels:
    app: python-service
spec:
  selector:
    app: python-service
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  type: ClusterIP
