# === Container Registry ===

# Registry для базовых Docker Hub образов (golang, python, redis, postgres и т.д.)
# По умолчанию: docker.io
# IMAGE_REGISTRY=docker.io

# Registry для MCR-образов (.NET SDK/Runtime)
# По умолчанию: mcr.microsoft.com
# MCR_REGISTRY=mcr.microsoft.com

# Registry для push собранных образов тестовых сервисов.
# Если не задан — образы получают локальный тег без registry-префикса.
# PUSH_REGISTRY=

# === Примеры для разных окружений ===

# Harbor (приватный registry):
# IMAGE_REGISTRY=harbor.example.com/docker
# MCR_REGISTRY=harbor.example.com/mcr
# PUSH_REGISTRY=harbor.example.com/library

# GitHub Container Registry:
# IMAGE_REGISTRY=docker.io
# PUSH_REGISTRY=ghcr.io/your-org
