apiVersion: v1
kind: Service
metadata:
  name: conformance-test-service-python
  namespace: dephealth-conformance
  labels:
    app: conformance-test-service-python
spec:
  selector:
    app: conformance-test-service-python
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  type: ClusterIP
