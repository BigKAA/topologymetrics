apiVersion: v1
kind: Service
metadata:
  name: conformance-test-service
  namespace: dephealth-conformance
  labels:
    app: conformance-test-service
spec:
  selector:
    app: conformance-test-service
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  type: ClusterIP
